export function SupportedContent() {
  return (
    <section className="bg-mist py-16 sm:py-20">
      <div className="mx-auto max-w-3xl px-4 sm:px-6">
        <h2 className="text-center text-3xl font-semibold text-ink">
          Supported content &amp; quality
        </h2>
        <p className="mt-4 text-center text-ink/60">
          Public Instagram videos and reels are supported. Available
          resolutions depend entirely on what the video was uploaded with -
          we display 360p, 480p, 720p, and 1080p only when they genuinely
          exist for that specific video.
        </p>
      </div>
    </section>
  );
}
